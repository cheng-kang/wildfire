/**
 * Plugin List
 *  1. each value should be a valid CDN link.
 *  2. each link should end without '/'.
 *     e.g.
 *       √ correct: 'https://unpkg.com/wf-pinned-comment'
 *       × wrong:   'https://unpkg.com/wf-pinned-comment/'
 */

const pluginList = {
  WfPinnedComment: 'https://unpkg.com/wf-pinned-comment',
};

export default pluginList;
